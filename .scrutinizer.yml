tools:
    php_sim: true
    php_pdepend: true
    php_analyzer: true
    external_code_coverage:
        timeout: 900
filter:
    excluded_paths:
        - 'app/*'
        - 'tests/*'
        - 'docs/*'
        - 'examples/*'
